<h1>Forms</h1>
<% @forms.each do |f| %>
    <% if !f.doc_description.empty? && !f.report_date.empty? %>
        <%= "#{f.doc_description} filed on: #{f.report_date}" %>
    <% end %>
    <br>

    <%= "See full details: #{f.uri}" if f.uri %>

    <br>
    <br>


<% end %>